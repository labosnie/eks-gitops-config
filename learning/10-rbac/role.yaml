apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  name: incident-pod-reader
  namespace: incident-platform
rules:
  - apiGroups:
      - ""
    resources:
      - pods
    verbs:
      - get
      - list
      - watch
