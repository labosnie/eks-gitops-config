replicaCount: 2

image:
  repository: incident-platform-api
  tag: "0.6.0"
  pullPolicy: IfNotPresent

config:
  appEnv: development

ingress:
  enabled: true
  className: nginx
  host: api.dev.example.com
